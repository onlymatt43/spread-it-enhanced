<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spread It — Rejoins la liste d'attente</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        * { box-sizing: border-box; }
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
        }
        .card {
            background: white;
            border-radius: 24px;
            padding: 3rem 2.5rem;
            max-width: 560px;
            width: 100%;
            box-shadow: 0 30px 80px rgba(0,0,0,0.25);
            text-align: center;
        }
        .logo {
            font-size: 3rem;
            margin-bottom: .5rem;
        }
        h1 {
            font-size: 2rem;
            font-weight: 800;
            color: #1e1b4b;
            margin-bottom: .5rem;
        }
        .tagline {
            color: #6b7280;
            font-size: 1.05rem;
            margin-bottom: 2rem;
        }
        .features {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: .8rem;
            margin-bottom: 2.5rem;
            text-align: left;
        }
        .feature {
            background: #f5f3ff;
            border-radius: 12px;
            padding: .85rem 1rem;
            font-size: .9rem;
            color: #374151;
            display: flex;
            align-items: flex-start;
            gap: .5rem;
        }
        .feature .ico { font-size: 1.2rem; line-height: 1; flex-shrink: 0; }
        .feature strong { display: block; font-size: .82rem; color: #1e1b4b; }
        .divider {
            border: none;
            border-top: 1px solid #e5e7eb;
            margin: 0 0 2rem;
        }
        .form-group { margin-bottom: 1rem; }
        .form-control {
            border-radius: 10px;
            padding: 12px 16px;
            font-size: 1rem;
            border: 1.5px solid #d1d5db;
            transition: border .2s;
        }
        .form-control:focus {
            border-color: #764ba2;
            box-shadow: 0 0 0 3px rgba(118,75,162,.15);
        }
        .btn-join {
            width: 100%;
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 14px;
            font-size: 1.05rem;
            font-weight: 700;
            cursor: pointer;
            transition: all .2s;
            box-shadow: 0 4px 20px rgba(102,126,234,.4);
            margin-top: .5rem;
        }
        .btn-join:hover { transform: translateY(-2px); box-shadow: 0 8px 28px rgba(102,126,234,.5); }
        .btn-join:disabled { opacity: .6; cursor: default; transform: none; }
        .success-msg {
            display: none;
            background: #d1fae5;
            border-radius: 12px;
            padding: 1.2rem;
            color: #065f46;
            font-weight: 600;
            margin-top: 1rem;
        }
        .error-msg {
            display: none;
            background: #fee2e2;
            border-radius: 12px;
            padding: .8rem;
            color: #991b1b;
            font-size: .9rem;
            margin-top: .8rem;
        }
        .back-link {
            margin-top: 1.5rem;
            font-size: .85rem;
            color: #9ca3af;
        }
        .back-link a { color: #764ba2; text-decoration: none; font-weight: 600; }
        .back-link a:hover { text-decoration: underline; }
        @media (max-width: 480px) {
            .features { grid-template-columns: 1fr; }
            .card { padding: 2rem 1.5rem; }
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="logo">🚀</div>
        <h1>Spread It</h1>
        <p class="tagline">Publie ton contenu partout en un clic — avec l'IA qui rédige pour toi.</p>

        <div class="features">
            <div class="feature">
                <span class="ico">🎬</span>
                <div><strong>Upload n'importe quoi</strong>Vidéo, photo, URL — tout fonctionne.</div>
            </div>
            <div class="feature">
                <span class="ico">✍️</span>
                <div><strong>L'IA rédige</strong>Textes optimisés pour chaque plateforme.</div>
            </div>
            <div class="feature">
                <span class="ico">📡</span>
                <div><strong>6 plateformes</strong>Instagram, TikTok, YouTube, LinkedIn, Facebook, Twitter.</div>
            </div>
            <div class="feature">
                <span class="ico">⚡</span>
                <div><strong>En quelques secondes</strong>De l'idée à la publication en moins d'une minute.</div>
            </div>
        </div>

        <hr class="divider">

        <p style="font-weight:700; color:#1e1b4b; font-size:1.05rem; margin-bottom:1.2rem;">
            Intéressé·e ? Laisse ton email — tu seras parmi les premiers à avoir accès.
        </p>

        <form id="joinForm">
            <div class="form-group">
                <input type="text" class="form-control" id="nameInput" placeholder="Ton prénom (optionnel)">
            </div>
            <div class="form-group">
                <input type="email" class="form-control" id="emailInput" placeholder="ton@email.com" required>
            </div>
            <button type="submit" class="btn-join" id="joinBtn">
                <i class="fas fa-paper-plane me-2"></i>Je veux accès à Spread It
            </button>
        </form>

        <div class="success-msg" id="successMsg">
            🎉 Super ! Tu es sur la liste. On te contacte très bientôt.
        </div>
        <div class="error-msg" id="errorMsg"></div>

        <p class="back-link">
            <a href="/" onclick="window.close(); return false;">&larr; Retour à l'accueil</a>
        </p>
    </div>

    <script>
    document.getElementById('joinForm').addEventListener('submit', function(e) {
        e.preventDefault();
        var btn = document.getElementById('joinBtn');
        var email = document.getElementById('emailInput').value.trim();
        var name = document.getElementById('nameInput').value.trim();
        var errEl = document.getElementById('errorMsg');
        var okEl = document.getElementById('successMsg');

        if (!email) { errEl.textContent = 'Entre ton email.'; errEl.style.display = 'block'; return; }

        btn.disabled = true;
        btn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>Envoi...';
        errEl.style.display = 'none';

        fetch('/api/join', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email: email, name: name })
        })
        .then(function(r) { return r.json(); })
        .then(function(data) {
            if (data.ok) {
                document.getElementById('joinForm').style.display = 'none';
                okEl.style.display = 'block';
            } else {
                errEl.textContent = data.error || 'Erreur, réessaie.';
                errEl.style.display = 'block';
                btn.disabled = false;
                btn.innerHTML = '<i class="fas fa-paper-plane me-2"></i>Je veux accès à Spread It';
            }
        })
        .catch(function() {
            errEl.textContent = 'Erreur réseau, réessaie.';
            errEl.style.display = 'block';
            btn.disabled = false;
            btn.innerHTML = '<i class="fas fa-paper-plane me-2"></i>Je veux accès à Spread It';
        });
    });
    </script>
</body>
</html>
