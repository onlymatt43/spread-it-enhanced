<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spread It — Rejoins la liste d'attente</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            background: #0d0d14;
            color: #f0f0f5;
            font-family: 'Inter', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
        }

        body::before {
            content: '';
            position: fixed;
            top: -150px;
            left: 50%;
            transform: translateX(-50%);
            width: 700px;
            height: 500px;
            background: radial-gradient(ellipse at center, rgba(139,92,246,.2) 0%, transparent 70%);
            pointer-events: none;
        }

        .card {
            position: relative;
            background: rgba(255,255,255,0.04);
            border: 1px solid rgba(255,255,255,0.08);
            border-radius: 24px;
            padding: 2.5rem 2rem;
            max-width: 500px;
            width: 100%;
            text-align: center;
        }

        .logo-wrap {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 52px; height: 52px;
            background: linear-gradient(135deg, #8b5cf6, #6366f1);
            border-radius: 16px;
            font-size: 1.5rem;
            margin-bottom: 1.2rem;
        }

        h1 {
            font-size: 1.8rem;
            font-weight: 800;
            letter-spacing: -1px;
            background: linear-gradient(135deg, #fff 30%, #c4b5fd 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: .4rem;
        }

        .tagline {
            color: rgba(240,240,245,0.5);
            font-size: .95rem;
            margin-bottom: 1.8rem;
            line-height: 1.55;
        }

        .features {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: .6rem;
            margin-bottom: 2rem;
            text-align: left;
        }
        .feature {
            background: rgba(139,92,246,.08);
            border: 1px solid rgba(139,92,246,.15);
            border-radius: 12px;
            padding: .8rem;
            font-size: .82rem;
            color: rgba(240,240,245,0.7);
            display: flex;
            align-items: flex-start;
            gap: .5rem;
        }
        .feature .ico { font-size: 1.1rem; line-height: 1; flex-shrink: 0; }
        .feature strong { display: block; font-size: .8rem; color: #c4b5fd; margin-bottom: .15rem; }

        .divider {
            border: none;
            border-top: 1px solid rgba(255,255,255,0.07);
            margin: 0 0 1.8rem;
        }

        .cta-label {
            font-size: .9rem;
            font-weight: 600;
            color: rgba(240,240,245,0.8);
            margin-bottom: 1.2rem;
        }

        .form-group { margin-bottom: .75rem; }
        .form-control {
            width: 100%;
            background: rgba(0,0,0,0.3);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 12px;
            color: #f0f0f5;
            padding: 12px 16px;
            font-size: .95rem;
            outline: none;
            transition: border .2s;
            font-family: 'Inter', sans-serif;
        }
        .form-control::placeholder { color: rgba(240,240,245,0.3); }
        .form-control:focus { border-color: #8b5cf6; }

        .btn-join {
            width: 100%;
            background: linear-gradient(135deg, #8b5cf6, #6366f1);
            color: white;
            border: none;
            border-radius: 14px;
            padding: 14px;
            font-size: 1rem;
            font-weight: 700;
            cursor: pointer;
            transition: all .2s;
            box-shadow: 0 4px 24px rgba(139,92,246,.35);
            margin-top: .5rem;
            font-family: 'Inter', sans-serif;
            letter-spacing: .1px;
        }
        .btn-join:hover { transform: translateY(-2px); box-shadow: 0 8px 32px rgba(139,92,246,.5); }
        .btn-join:disabled { opacity: .4; cursor: default; transform: none; box-shadow: none; }

        .success-msg {
            display: none;
            background: rgba(16,185,129,.12);
            border: 1px solid rgba(16,185,129,.25);
            border-radius: 12px;
            padding: 1.2rem;
            color: #6ee7b7;
            font-weight: 600;
            margin-top: 1rem;
            font-size: .95rem;
        }
        .error-msg {
            display: none;
            background: rgba(239,68,68,.1);
            border: 1px solid rgba(239,68,68,.2);
            border-radius: 10px;
            padding: .8rem;
            color: #fca5a5;
            font-size: .85rem;
            margin-top: .8rem;
        }

        .back-link {
            margin-top: 1.5rem;
            font-size: .82rem;
            color: rgba(240,240,245,0.35);
        }
        .back-link a {
            color: #a78bfa;
            text-decoration: none;
            font-weight: 600;
        }
        .back-link a:hover { text-decoration: underline; }

        @media (max-width: 420px) {
            .features { grid-template-columns: 1fr; }
            .card { padding: 2rem 1.2rem; }
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="logo-wrap">🚀</div>
        <h1>Spread It</h1>
        <p class="tagline">Publie ton contenu partout en un clic —<br>avec l'IA qui rédige pour toi.</p>

        <div class="features">
            <div class="feature">
                <span class="ico">🎬</span>
                <div><strong>Upload n'importe quoi</strong>Vidéo, photo, URL — tout fonctionne.</div>
            </div>
            <div class="feature">
                <span class="ico">✍️</span>
                <div><strong>L'IA rédige</strong>Textes optimisés pour chaque plateforme.</div>
            </div>
            <div class="feature">
                <span class="ico">📡</span>
                <div><strong>6 plateformes</strong>Instagram, TikTok, YouTube, LinkedIn, Facebook, Twitter.</div>
            </div>
            <div class="feature">
                <span class="ico">⚡</span>
                <div><strong>En quelques secondes</strong>De l'idée à la publication en moins d'une minute.</div>
            </div>
        </div>

        <hr class="divider">

        <p class="cta-label">Laisse ton email — tu seras parmi les premiers à avoir accès.</p>

        <form id="joinForm">
            <div class="form-group">
                <input type="text" class="form-control" id="nameInput" placeholder="Ton prénom (optionnel)">
            </div>
            <div class="form-group">
                <input type="email" class="form-control" id="emailInput" placeholder="ton@email.com" required>
            </div>
            <button type="submit" class="btn-join" id="joinBtn">
                <i class="fas fa-paper-plane" style="margin-right:.5rem"></i>Je veux accès à Spread It
            </button>
        </form>

        <div class="success-msg" id="successMsg">
            🎉 Super ! Tu es sur la liste. On te contacte très bientôt.
        </div>
        <div class="error-msg" id="errorMsg"></div>

        <p class="back-link">
            <a href="/" onclick="window.close(); return false;">&larr; Retour à l'accueil</a>
        </p>
    </div>

    <script>
    document.getElementById('joinForm').addEventListener('submit', function(e) {
        e.preventDefault();
        var btn = document.getElementById('joinBtn');
        var email = document.getElementById('emailInput').value.trim();
        var name = document.getElementById('nameInput').value.trim();
        var errEl = document.getElementById('errorMsg');
        var okEl = document.getElementById('successMsg');

        if (!email) { errEl.textContent = 'Entre ton email.'; errEl.style.display = 'block'; return; }

        btn.disabled = true;
        btn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>Envoi...';
        errEl.style.display = 'none';

        fetch('/api/join', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email: email, name: name })
        })
        .then(function(r) { return r.json(); })
        .then(function(data) {
            if (data.ok) {
                document.getElementById('joinForm').style.display = 'none';
                okEl.style.display = 'block';
            } else {
                errEl.textContent = data.error || 'Erreur, réessaie.';
                errEl.style.display = 'block';
                btn.disabled = false;
                btn.innerHTML = '<i class="fas fa-paper-plane me-2"></i>Je veux accès à Spread It';
            }
        })
        .catch(function() {
            errEl.textContent = 'Erreur réseau, réessaie.';
            errEl.style.display = 'block';
            btn.disabled = false;
            btn.innerHTML = '<i class="fas fa-paper-plane me-2"></i>Je veux accès à Spread It';
        });
    });
    </script>
</body>
</html>
